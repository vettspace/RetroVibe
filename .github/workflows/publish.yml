name: Publish RetroVibe Theme

on:
  push:
    branches:
      - main

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '16'
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Build plugin
        run: npm run build

      - name: Configure npm registry
        run: npm config set registry https://registry.npmjs.org/

      - name: Publish to npm
        run: npm publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Send message to Telegram
        if: success() # Отправить сообщение только при успешной публикации
        uses: appleboy/telegram-action@master
        with:
          to: ${{ secrets.TELEGRAM_TO }}
          token: ${{ secrets.TELEGRAM_TOKEN }}
          message: |
            ✅ Публикация RetroVibe выполнена!
            📝 Коммит: ${{ github.event.head_commit.message }}
            🌓 Insomnia: https://insomnia.rest/plugins/insomnia-plugin-retro-vibe-theme
            🔗 NPM: https://www.npmjs.com/package/insomnia-plugin-retro-vibe-theme
            📦 GitHub: https://github.com/vettspace/RetroVibe